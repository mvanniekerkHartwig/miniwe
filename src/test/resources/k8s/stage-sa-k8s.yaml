---
apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "wf-1-0-0-ex-simple-stage-out"
  namespace: "namespace"
spec:
  accessModes:
  - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"
  storageClassName: "standard"
---
apiVersion: "batch/v1"
kind: "Job"
metadata:
  name: "wf-1-0-0-ex-simple-stage"
  namespace: "namespace"
spec:
  backoffLimit: 1
  template:
    spec:
      containers:
      - image: "eu.gcr.io/hmf-build/image:1.0.0"
        name: "wf-1-0-0-ex-simple-stage"
        volumeMounts:
        - mountPath: "/out"
          name: "wf-1-0-0-ex-simple-stage-out"
      restartPolicy: "Never"
      serviceAccountName: "my-sa"
      volumes:
      - name: "wf-1-0-0-ex-simple-stage-out"
        persistentVolumeClaim:
          claimName: "wf-1-0-0-ex-simple-stage-out"
          readOnly: false
---
apiVersion: "batch/v1"
kind: "Job"
metadata:
  name: "wf-1-0-0-ex-simple-stage-out"
  namespace: "namespace"
spec:
  backoffLimit: 1
  template:
    spec:
      containers:
      - command:
        - "sh"
        - "-c"
        - "gsutil -m rsync -r /out gs://run-wf-1-0-0-ex/simple-stage"
        image: "eu.gcr.io/hmf-build/google/cloud-sdk:425.0.0"
        name: "simple-stage-copier"
        volumeMounts:
        - mountPath: "/out"
          name: "wf-1-0-0-ex-simple-stage-out"
      restartPolicy: "Never"
      serviceAccountName: "serviceAccount"
      volumes:
      - name: "wf-1-0-0-ex-simple-stage-out"
        persistentVolumeClaim:
          claimName: "wf-1-0-0-ex-simple-stage-out"
          readOnly: false
